import { PlayerInterestPipe } from './player-interest.pipe';

describe('PlayerInterestPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerInterestPipe();
    expect(pipe).toBeTruthy();
  });
});
